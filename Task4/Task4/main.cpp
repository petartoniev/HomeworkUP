#include <iostream>

using namespace std;

int main()
{
    int a,b;
    cout<<"input first number: "; cin>>a;
    cout<<"input second number: "; cin>>b;
    int c=(a>b ? a : b);
    cout<<"result: "<<(c<0 ? z*(-1): c);
    return 0;
}
